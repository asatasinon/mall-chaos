apiVersion: v1
kind: Secret
metadata:
  name: traffic-lifecycle-secret
  namespace: castrel
type: Opaque
stringData:
  # Keep disabled until this value is replaced through the deployment secret workflow.
  TRAFFIC_LIFECYCLE_LOGIN_ENABLED: "false"
  TRAFFIC_LIFECYCLE_ACCOUNTS: "[]"